diff -Naur lxqt-runner-0.9.0/providers.cpp lxqt-runner-0.9.0.tpg/providers.cpp
--- lxqt-runner-0.9.0/providers.cpp	2015-01-29 15:24:04.000000000 +0000
+++ lxqt-runner-0.9.0.tpg/providers.cpp	2015-03-15 13:15:41.229324745 +0000
@@ -90,7 +90,12 @@
     if (program.isEmpty())
         return false;
 
-    return QProcess::startDetached(program, args);
+    QProcess* p = new QProcess;
+    p->setStandardInputFile(QProcess::nullDevice());
+    p->setProcessChannelMode(QProcess::ForwardedChannels);
+    QObject::connect(p, SIGNAL(finished(int, QProcess::ExitStatus)), p, SLOT(deleteLater()));
+    p->start(program, args);
+    return p->waitForStarted();
 }
 
 
